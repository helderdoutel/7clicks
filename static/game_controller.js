function recarregaPagina(url){
    if(url){
        //faz o request e retorna o html da página específica
    }else{
        // retorna a pagina aleatoria
    }
}

function comecar_jogo(){
    var init = document.getElementById('pagina_inicio');
    recarregaPagina(init.value);
}